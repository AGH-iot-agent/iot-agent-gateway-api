server:
  port: 8080

spring:
  application:
    name: iot-agent-gateway-api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
